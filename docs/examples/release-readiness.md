# Release Readiness Example

User asks whether a build can ship.

Expected flow:

1. Load `game-studio-release`.
2. Use `game-studio-qa` evidence for smoke, regression, performance, and known issues.
3. Apply release, QA, producer, and community lenses.
4. Return a release verdict, player notes, and hotfix criteria.
